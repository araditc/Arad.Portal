@*@model Models.Pagination<Models.Slider.LayersListGridView>
@{
    Layout = null;
}

@{
    <div id="page-data" data-current="@Model.CurrentPage" data-total="@Model.TotalPage" data-size="@Model.PageSize">
        @{
            if (Model.Data.Count != 0)
            {
                int i = (Model.CurrentPage - 1) * Model.PageSize;

                @foreach (var item in Model.Data)
                {
                    <div class="table-row">
                        <div class="card-table">
                            <div class="row m-0">
                                <div class="table-col col-xs-12 col-md-1 d-none d-lg-block">
                                    <span>@(++i)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title">نوع:</span>
                                    <span>
                                        @item.Type
                                    </span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-3">
                                    <span class="mo-title">محتوا:</span>
                                    <span>@item.Content</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title">  Link:</span>
                                    <span>@(item.Link == null ? "---" : item.Link)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title">  Position-Top:</span>
                                    <span>@(item.Position.Top == null ? "---" : item.Position.Top)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title"> Position-Right:</span>
                                    <span>@(item.Position.Right == null ? "---" : item.Position.Top)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title"> Position-Bottom:</span>
                                    <span>@(item.Position.Bottom == null ? "---" : item.Position.Top)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-1">
                                    <span class="mo-title">  Position-Left:</span>
                                    <span>@(item.Position.Left == null ? "---" : item.Position.Top)</span>
                                </div>
                                <div class="table-col col-xs-12 col-lg-2">
                                    <div class="">
                                        @*@if (ViewBag.Permissions["Details"])
                {*@

                                        <a id="editRecord" asp-action="EditLayer" asp-controller="Slider" asp-area="Admin" asp-route-sliderId="@ViewBag.SliderId" asp-route-slideId="@ViewBag.SlideId" asp-route-layerId="@item.Id" class="btn btn-outline-primary btn-xs">
                                            جزییات
                                        </a>

                                        @*}
                @if (ViewBag.Permissions["Remove"])
                {*@
                                        <button id="deleteRecord" data-id="@item.Id" href="#" data-toggle="modal" type="button" class="btn btn-outline-danger btn-xs">
                                            حذف
                                        </button>
                                        @*}*@
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>
                }
            }
            else
            {
                <div class="empty text-center">اطلاعاتی برای نمایش وجود ندارد.</div>
            }
        }
    </div>
}
*@