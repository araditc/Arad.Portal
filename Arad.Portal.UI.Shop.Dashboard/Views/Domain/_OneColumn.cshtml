@using Arad.Portal.DataLayer.Models.DesignStructure;
@model Arad.Portal.DataLayer.Models.DesignStructure.RowContent
@{
    var section = "";
    if(@ViewBag.RowNumber.ToString().StartsWith("1"))
    {
        section = "headerSection";
    }else if(@ViewBag.RowNumber.ToString().StartsWith("2"))
    {
        section = "containerSection";
    }else if(@ViewBag.RowNumber.ToString().StartsWith("3"))
    {
        section = "footerSection";
    }
    var guid = @ViewBag.Guid;
    ColContent firstColData = null;
    if(Model != null)
    {
       firstColData = Model.ColsContent.First();
    }
}
<div class="row sectionRow mx-2" rn="@ViewBag.RowNumber" rowCustomClasses="" guid="@guid">
    <div class="col-12 border border-1" colnum="1" did='@ViewBag.DomainId' cnt="1">
         <partial name="_InnerSection.cshtml" model='new ColContent(){ColNumber = 1,Section = section, ColumnClassNames = "col-12", RowGuid = guid , ColData =  firstColData != null ? firstColData.ColData : ""}'/>
    </div>
</div>