@using Arad.Portal.Models.Shared.ContentCategory
@using Arad.Portal.Models.UI
@model CommonViewModel

<div class="row">
    @foreach (ContentCategoryDto item in Model.Categories)
    {
        <div class="col-6 col-md-3 catSection">

            <div class="card m-4 border-0" >
               
                <div class="card-body text-center" >
                    <a href="/category/@item.CategoryCode" class="btn bg-gradient text-decoration-none text-dark">
                        <h6 class="card-title">@(item.CategoryNames.Any(p => p.LanguageId == ViewBag.CurLangId) ? 
                            item.CategoryNames.FirstOrDefault(p=>p.LanguageId == ViewBag.CurLangId).Name : item.CategoryNames.FirstOrDefault().Name)</h6>
                    </a>
                </div>
            </div>

        </div>
    }
</div>
 